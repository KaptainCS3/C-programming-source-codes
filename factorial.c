#include<stdio.h>

int main()
{
Int factorial (int z)
{
              Printf("error message");
           }
             break;
Else
           {
              If((z==0) || (z==1)
                {
                    return 1;    
                }
else return z*factorial (z-1);
                }
            }
    return 0;
}